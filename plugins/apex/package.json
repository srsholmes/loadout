{
  "name": "@loadout/plugin-apex",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "react": "^18.3.0",
    "react-icons": "^5.6.0",
    "@loadout/ui": "workspace:*",
    "@loadout/types": "workspace:*",
    "@loadout/exec": "workspace:*",
    "@loadout/plugin-storage": "workspace:*",
    "@loadout/devices": "workspace:*",
    "@loadout/wake": "workspace:*"
  },
  "plugin": {
    "id": "apex",
    "name": "OneXPlayer",
    "description": "OneXPlayer device fixes: recover the internal gamepad when its xHCI controller dies on resume \u2014 manually or automatically on every wake \u2014 and block the fingerprint reader from waking the device on a light touch; set the gamepad's global rumble intensity.",
    "permissions": {
      "commands": [
        "tee",
        "lsusb",
        "dmesg",
        "dbus-monitor",
        "systemctl",
        "sh",
        "udevadm",
        "steamos-readonly",
        "update-grub"
      ],
      "filesystem": [
        "/sys/bus/hid/devices"
      ]
    },
    "category": "Device",
    "subtitle": "Gamepad, vibration + fingerprint",
    "target": {
      "type": "overlay"
    }
  }
}
